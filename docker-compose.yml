services:
  labes-front:
    container_name: labes-ui

    build:
      context: .
      dockerfile: Dockerfile.homolog

    restart: unless-stopped

    ports:
      - '4200:80'

    networks:
      - labes-network

networks:
  labes-network:
    external: true
